<!DOCTYPE html>

<html lang="en">

  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link href="https://fonts.googleapis.com/css?family=Nunito&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/register.css" media="screen">
    <title>Facefluff Registry</title>
  </head>
  
  <body>
    
    <%- include("./../general/nav_login") %>

    <main>
      <%- include("./../general/flash") %>
      <div id="main_panel">
        <form action="/login/register" method="POST" enctype="multipart/form-data">
          Email:* 
          <%if (errores != null && errores.user_email) {%>
            <span class="err-field"> <%=errores.user_email.msg%></span>
          <% } %>
          <input type="text" name="user_email">
          Password:* 
          <%if (errores != null && errores.user_password) {%>
            <span class="err-field"> <%=errores.user_password.msg%></span>
          <% } %>
          <input type="password" name="user_password">
          Nombre:* 
          <%if (errores != null && errores.user_name) {%>
            <span class="err-field"> <%=errores.user_name.msg%></span>
          <% } %>
          <input type="text" name="user_name">
          <div id="form_genre">
            Sexo:*
            <%if (errores != null && errores.user_genre) {%>
              <span class="err-field"> <%=errores.user_genre.msg%></span>
            <% } %>
            <input type="radio" name="user_genre" value="hombre"> Hombre
            <input type="radio" name="user_genre" value="mujer"> Mujer
            <input type="radio" name="user_genre" value="otro"> Otro
          </div>
          Fecha de nacimiento:* 
          <%if (errores != null && errores.user_birthday) {%>
            <span class="err-field"> <%=errores.user_birthday.msg%></span>
          <% } %>
          <input type="date" name="user_birthday">
          Imagen de perfil: 
          <input type="file" name="user_img" accept=".jpg, .jpeg, .png">
          <input type="submit" value="Conectar" class="button_control">
        </form>
      </div>
    </main>
  </body>

</html>
